---
summary: "CLI reference for `localclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `localclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
localclaw uninstall
localclaw uninstall --all --yes
localclaw uninstall --dry-run
```
